/* Solved by Sakhawat Hossain Mahin, CSE-28th, SMUCT */
#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int T;
    cin>>T;
    while(T--)
    {
        int n;
        cin>>n;
        int a[n];
        int b[n];
        vector<pair<int, int>>v;
        vector<int>ans;

        for(int i = 0; i<n; i++)
            cin>>a[i];
        for(int i = 0; i<n; i++){
            cin>>b[i];
        }
        

    }
    return 0;
}