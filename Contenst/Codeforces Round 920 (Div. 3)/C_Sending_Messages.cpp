/* Solved by Sakhawat Hossain Mahin, CSE-28th, SMUCT */
#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int T;
    cin >> T;
    while (T--)
    {
        int m, c, b, to;
        cin >> m >> c >> b >> to;
        int ar[m];
        vector<int> v;
        for (int i = 0; i < m; i++)
            cin >> ar[i];

        for(int i = 0; i<m; i++)
        {
            if((ar[i] - ) < b)
                c-=
        }
    }
    return 0;
}