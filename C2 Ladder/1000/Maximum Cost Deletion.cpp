#include<bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);

    int t;
    cin>>t;

    while(t--)
    {
        int n, a, b;
        cin>>n>>a>>b;

        int subSize;

        if(n%2 == 0)
        {
            subSize = n / b;
        }
        else
        {
            subSize = n + b;
        }

        string s;
        cin>>s;

        for(int i=0; i<n; i++)
        {
            string temp = s.substr( )
        }
    }
}